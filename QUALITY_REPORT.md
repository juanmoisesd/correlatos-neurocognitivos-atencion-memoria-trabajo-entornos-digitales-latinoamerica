# Quality Report

The dataset has been validated for:
- Completeness: No missing critical fields.
- Validity: All values within expected ranges (see `schema.json`).
- Consistency: Data types are consistent across records.

Detailed validation results are in `quality/validation_report.json`.
